import React from 'react'
import styles from "../../../app/CSS/founders.module.css";

export default function Content() {
  return (
    <div className={`${styles.Content}`}>
        <h4 className={`${styles.title}`}>Founding Members</h4>
        <h2 className={`${styles.subtitle}`}>GALS: Empowering Innovators, Visionaries, and Leaders in Technology.</h2>
    </div>
  )
}
